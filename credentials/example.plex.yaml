# Baseurl can be just http://IP:port, but can also be e.g. a domain if a reverse proxy is used
baseurl: "http://localhost:32400"

# Token can be retrieved as explained e.g. here:
# https://support.plex.tv/articles/204059436-finding-an-authentication-token-x-plex-token/
token: "API_TOKEN"
